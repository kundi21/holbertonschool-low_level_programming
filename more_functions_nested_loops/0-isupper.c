#include "main.h"
#include <stdio.h>

/**
* main - check the code.
*
* int _isupper(int c); -  a function that checks for uppercase character.
*
* Returns 1 if c is uppercase
* Returns 0 otherwise
 */
int _isupper(int c);
