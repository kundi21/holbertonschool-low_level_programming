#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase
 (*
 * Return: 0 if success
 */
int main(void)
{
	int (x);
	char (x == 'a');
	char (x <= "z\n");
	char (x++);
	{
	putchar(x);
	}
	return (0);

}
