#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase
 (*
 * Return: 0 if success
 */
int main(void)
{
	for (int x = 'a'; x <= 'z'; x++)
	{
	putchar(x);
	}
	return (0);

}
